# Doc Maintenance Rules

- Current-truth docs only describe the system as it exists now.
- Put temporary plans, proposals, and migration notes in `docs/working/`.
- Put developer-facing technical truth in `docs/developer/`.
- Put user-facing guidance in `docs/user/`.
- Remove compatibility history from formal docs.
